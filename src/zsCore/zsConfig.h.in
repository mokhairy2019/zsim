/** @file zsConfig.h

@brief Provides preprocessor directives configuration of zsim

It is autogenerated from zsConfig.h.in during the cmake
configuration of zsim. If you need to make changes edit the
original file in the sources and NOT THIS FILE.

*/
#pragma once

/** Define the G+Smo version number. */
#define ZSIM_VERSION    "@zsim_VERSION@"
#define ZSIM_MAJOR      @zsim_VERSION_MAJOR@
#define ZSIM_MINOR      @zsim_VERSION_MINOR@
#define ZSIM_PATCH      @zsim_VERSION_PATCH@

/** Define default coefficient type. */
#define real_t           @ZSIM_COEFF_TYPE@

/** Define default index type. */
#define index_t          @ZSIM_INDEX_TYPE@

/** Define default dimension type. */
#define short_t          @ZSIM_INDEX_TYPE@ //short

/** Define the file data directory. */
#define ZSIM_DATA_DIR "@ZSIM_DATA_DIR@"

/** Define the search paths. */
#define ZSIM_SEARCH_PATHS "@ZSIM_SEARCH_PATHS@"

/** Define the file configuration directory. */
#define ZSIM_CONFIG_DIR "@ZSIM_CONFIG_DIR@"

/* Enabled Extensions */
#cmakedefine ZSIM_WITH_ADIFF
#cmakedefine ZSIM_WITH_CODIPACK
#cmakedefine ZSIM_WITH_IPOPT
#cmakedefine ZSIM_WITH_MPFR
#cmakedefine ZSIM_WITH_MPI
#cmakedefine ZSIM_WITH_MPQ
#cmakedefine ZSIM_WITH_ONURBS
#cmakedefine ZSIM_WITH_OCC
#cmakedefine ZSIM_WITH_PARDISO
#cmakedefine ZSIM_WITH_PASTIX
#cmakedefine ZSIM_WITH_PSOLID
#cmakedefine ZSIM_WITH_SPECTRA
#cmakedefine ZSIM_WITH_SUPERLU
#cmakedefine ZSIM_WITH_TAUCS
#cmakedefine ZSIM_WITH_TRILINOS
#cmakedefine ZSIM_WITH_UMFPACK
#cmakedefine ZSIM_WITH_UNUM

#ifdef ZSIM_WITH_MPI
#define __PARALLEL_MODE
#endif

/* Only include new types here that can be set as real_t */

#ifdef ZSIM_WITH_MPFR
#include <mpreal.h>
#endif

#ifdef ZSIM_WITH_MPQ
#include <gmpxx.h>
#include <unsupported/Eigen/MPQClassExtra>
#endif

#ifdef ZSIM_WITH_UNUM
#include <gsUnum/gsUnum.h>
#endif

/* Determine if only header files should be used. */
#cmakedefine ZSIM_BUILD_LIB
//#cmakedefine ZSIM_HAS_EXTERN_TEMPLATES

/* Debug settings. */
#cmakedefine ZSIM_EXTRA_DEBUG
#cmakedefine ZSIM_WARNINGS

/**
 * @name Eigen options - MUST be defined before Eigen is included
 * @{
 */

/*
   Eigen options -- these MUST be defined before Eigen is included.

   See:
   http://eigen.tuxfamily.org/dox/TopicPreprocessorDirectives.html
*/

/*  Index type */
/** Eigen options -- these MUST be defined before Eigen is included.\n
   See: http://eigen.tuxfamily.org/dox/TopicPreprocessorDirectives.html

   Index type
*/
#define EIGEN_DEFAULT_DENSE_INDEX_TYPE  index_t

/** Eigen options -- these MUST be defined before Eigen is included.\n
   See: http://eigen.tuxfamily.org/dox/TopicPreprocessorDirectives.html

   Index type
*/
#define EIGEN_DEFAULT_SPARSE_INDEX_TYPE index_t

/** Eigen options -- these MUST be defined before Eigen is included.\n
   See: http://eigen.tuxfamily.org/dox/TopicPreprocessorDirectives.html

 Matrix storage order
*/
#define EIGEN_DEFAULT_TO_COL_MAJOR

/** Eigen options -- these MUST be defined before Eigen is included.\n
  See: http://eigen.tuxfamily.org/dox/TopicPreprocessorDirectives.html

  Compile-time static assertions are replaced by runtime assertions;
  this saves compilation time. */
#define EIGEN_NO_STATIC_ASSERT

/** Eigen options -- these MUST be defined before Eigen is included.\n
    See http://eigen.tuxfamily.org/dox/TopicUsingIntelMKL.html */
#cmakedefine EIGEN_USE_MKL_ALL

/**@}*/

